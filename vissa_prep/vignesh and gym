# Read the input values
X, Y, Z = map(int, input().split())

# Check the conditions and print the appropriate result
if Z < X:
    print(0)  # Can't even join the gym
elif Z >= X + Y:
    print(2)  # Can join the gym and hire a personal trainer
else:
    print(1)  # Can only join the gym

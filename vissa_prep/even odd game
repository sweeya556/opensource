# Read the number as a string
N = input().strip()

# Calculate the sum of the digits
digit_sum = sum(int(digit) for digit in N)

# Check if the sum is even or odd and print the winner
if digit_sum % 2 == 0:
    print("Vignesh")
else:
    print("Charan")
